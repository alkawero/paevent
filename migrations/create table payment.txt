CREATE TABLE `seminar_db`.`payment` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `event_id` INT NULL,
  `price` DECIMAL(10) NULL,
  `status` INT NULL,
  `bukti_bayar` VARCHAR(200) NULL,
  PRIMARY KEY (`id`));
